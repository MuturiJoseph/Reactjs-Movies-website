import React from "react";

const MovieButton = ({ history }) => {

  return (
    <div>
      <button
        className="btn btn-primary"
      >
        New Movie
      </button>
    </div>
  );
};

export default MovieButton;